{
  "name": "gasflow-desktop",
  "version": "1.1.1",
  "private": true,
  "author": "GasFlow",
  "description": "GasFlow Desktop — container Electron do frontend original, com backend FastAPI, agente de integração e WhatsApp rodando localmente.",
  "main": "dist/main/index.js",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "build": "tsc -p tsconfig.json",
    "start": "electron .",
    "build:win": "npm run build && npm run build --prefix ../agent && npm run build --prefix ../whatsapp && npm run build --prefix ../frontend && electron-builder --win",
    "dist": "npm run build && npm run build --prefix ../agent && npm run build --prefix ../whatsapp && npm run build --prefix ../frontend && electron-builder --win"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "electron": "^44.3.0",
    "electron-builder": "^26.15.3",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "electron-log": "^5.4.4",
    "electron-updater": "^6.8.9"
  }
}
