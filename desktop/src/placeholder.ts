/**
 * Placeholder de build — os fontes reais do main process (src/main/*.ts)
 * não estão no repositório nesta árvore (apenas dist/ compilado).
 * Este arquivo existe apenas para que `npm run typecheck`/`npm run build`
 * tenham um input válido até os fontes serem restaurados. Não altera o app:
 * o Electron carrega dist/main/index.js (build prévio).
 */
export {}
