fastapi
uvicorn[standard]

sqlalchemy
psycopg2-binary

pydantic
python-dotenv

httpx
alembic

# Auth (hash de senha)
bcrypt

# Relatórios (PDF — relatório diário/impressão)
reportlab

# PIX (BR Code + QR Code)
qrcode
Pillow

# Rate limiting distribuído (Redis — produção multi-worker; opcional)
redis

# Métricas Prometheus (/metrics)
prometheus-client>=0.20
