fastapi
pyinstaller==6.22.2  # empacotamento do gasflow-backend.exe (desktop)
uvicorn[standard]

# Upload de arquivos (multipart — UploadFile do FastAPI; VCF import).
# Fixada: 0.0.12 tem múltiplos advisories (PYSEC-2026-1851/1852/3036–3040).
python-multipart>=0.0.31

sqlalchemy
psycopg2-binary

pydantic
python-dotenv

httpx
alembic

# Auth (hash de senha)
bcrypt

# Relatórios (PDF — relatório diário/impressão)
reportlab

# PIX (BR Code + QR Code)
qrcode
Pillow

# Rate limiting distribuído (Redis — produção multi-worker; opcional)
redis

# Métricas Prometheus (/metrics)
prometheus-client>=0.26.0

# Fusos IANA (zoneinfo). No Linux o tz db do sistema cobre; no Windows
# (exe PyInstaller e ambiente de teste) o pacote com os dados é obrigatório.
tzdata
