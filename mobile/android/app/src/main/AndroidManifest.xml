<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Rastreamento F2.5 (decisão B2): captura só com o app em primeiro
         plano, durante rota e dentro da janela de trabalho. Sem
         ACCESS_BACKGROUND_LOCATION — sem foreground service no MVP. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Cleartext HTTP é intencional: o modo LAN resolve a base URL como
         http://{ip}:{port} em runtime (src/logic/connection.ts) e, desde o
         Android 9 (targetSdk >= 28), o padrão é bloquear. Sem isto o release
         não loga na rede do depósito — só o debug logava, porque o manifest
         de debug ligava o flag sozinho. A nuvem (relay) segue em HTTPS. -->
    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:usesCleartextTraffic="true"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
