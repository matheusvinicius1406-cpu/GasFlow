name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:

  backend:
    name: Backend (ruff + pytest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements*.txt
      - name: Install dependencies
        run: |
          pip install -r requirements.txt -r requirements-dev.txt
      - name: Lint (ruff)
        run: ruff check app tests
      - name: Test (pytest)
        run: python -m pytest tests/ -q

  frontend:
    name: Frontend (typecheck + build + tests)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Build (tsc + vite)
        run: npm run build
      - name: Test (vitest)
        run: npm test

  whatsapp:
    name: WhatsApp service (typecheck + build + tests)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: whatsapp
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          # node:sqlite (src/db.ts) exige Node >= 22.5; imagem prod usa 24
          node-version: "24"
          cache: npm
          cache-dependency-path: whatsapp/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Build (tsc)
        run: npm run build
      - name: Test
        run: npm test

  e2e:
    name: E2E (Playwright, full stack)
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    steps:
      - uses: actions/checkout@v4
      - name: Boot E2E stack (postgres + redis + backend + frontend/nginx)
        run: docker compose -f docker-compose.e2e.yml up -d --build
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: e2e/package-lock.json
      - name: Install Playwright + chromium
        working-directory: e2e
        run: |
          npm ci
          npx playwright install --with-deps chromium
      - name: Run E2E tests
        working-directory: e2e
        env:
          ADMIN_PASSWORD: admin123
        run: npx playwright test
      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: e2e/test-results/
          retention-days: 7

  trivy:
    name: Trivy (scan de vulnerabilidades)
    runs-on: ubuntu-latest
    needs: [backend, frontend, whatsapp]
    steps:
      - uses: actions/checkout@v4
      - name: Build images
        run: |
          docker build -t gasflow-backend ./backend
          docker build -t gasflow-frontend ./frontend
          docker build -t gasflow-whatsapp ./whatsapp
      - name: Scan backend
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: gasflow-backend
          severity: CRITICAL,HIGH
          exit-code: "1"
      - name: Scan frontend
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: gasflow-frontend
          severity: CRITICAL,HIGH
          exit-code: "1"
      - name: Scan whatsapp
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: gasflow-whatsapp
          severity: CRITICAL,HIGH
          exit-code: "1"
      # Nota: SARIF upload (github/codeql-action/upload-sarif) exige repo
      # privado ou GitHub Advanced Security. Log no job basta para CI público.
